/* main controls the program. 
 * create two global arrays, one for substitutes, and one for the story,
 * then create an empty global for storyWithSpaces. Run the program. 
 * @param none
 * @return none
 */


 // Call encodeStory
 // Call spaceMarks
 // let numberedList = subSpeech()
 // call showSpeechList with numberedList as a parameter
 // alert the user that setup is complete and that the player can now start playing
 // call rebuildStory, then create a new variable that connects the story array together with spaces, defining storyWithSpaces as join
 // showStory(), then thank the player and end.

 /* getStory()
  * user input is stored as a new variable, that variable is returned.
  * @param none
  * @return storyString
  */

 /* encodeStory(storyString)
 * create a copy of storyString as a new variable, story array is equal to storyString being split by spaces
 * story = spaceMarks(story)
 * return string
 * @param storyString
 * @return story, string
 */